spring:
  profiles:
    active: dev
server:
  port: 8080
  servlet:
    context-path: /api
    session:
      cookie:
        max-age: 36000
        path: /
        name: SESSIONID
      timeout: 36000
resource:
  static:
    images: src/main/resources/static/images/default-thumbnail.webp
logging:
  config: src/main/resources/logback-spring.xml